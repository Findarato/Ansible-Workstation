#!/usr/bin/env ansible-playbook
---
- name: Install Zsh on {{ ansible_distribution }}
  dnf:
    state: present
    name: "{{item}}"
  with_items:
    - zsh

- name: use oh-my-zsh
  git:
    repo=https://github.com/robbyrussell/oh-my-zsh.git
    dest="/home/{{ local_user }}/.oh-my-zsh"